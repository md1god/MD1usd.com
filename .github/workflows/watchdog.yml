name: Watchdog

on:
  schedule:
    - cron: '*/5 * * * *'   # يشتغل كل 5 دقايق
  workflow_dispatch:         # تقدر تشغله يدويًا من GitHub

jobs:
  watchdog-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run watchdog script
        run: |
          echo "✅ Watchdog running..."
        
