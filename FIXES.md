# MD1USD.com — سجل التعديلات وما تبقى

## ✅ تم إنجازه في هذه الجولة

- إصلاح مشكلة حرجة: مجلدات `images/`, `audio/`, `videos/` وملفات
  `robots.txt`, `sitemap.xml` كانت في جذر المشروع بدل `public/`، فكانت
  تختفي بالكامل من كل نشر فعلي على GitHub Pages. تم نقلها جميعاً إلى
  `public/` والتحقق من ظهورها في `dist/` بعد البناء.
- إصلاح شعار الموقع: كان Navbar وFooter يعرضان رمز "₹" بدل الشعار
  الحقيقي (`md1usd_logo.png`) الذي كان موجوداً وغير مستخدم.
- إصلاح أيقونة المتصفح (favicon): كانت تشير لملف `/vite.svg` غير موجود
  إطلاقاً (404 دائم). استُبدلت بالشعار الحقيقي.
- إصلاح مشغّل الصوت: كان يحاول التشغيل التلقائي (autoplay) الذي تمنعه
  كل المتصفحات الحديثة بصمت. الآن يعرض زر "اضغط للاستئناف" واضحاً بدل
  الفشل الصامت، وتم توحيد التحكم بين زر الصفحة الرئيسية والزر العائم
  لمنع تشغيل مقطعين صوتيين في آن واحد.
- إصلاح خلل تجربة مستخدم خطير: المقدمة السينمائية (12 ثانية) كانت تُجبر
  المستخدم على الانتظار حتى عند فتح رابط داخلي مباشر (مثل `/security`
  من نتيجة بحث أو مشاركة). الآن تظهر فقط عند زيارة الصفحة الرئيسية،
  ومرة واحدة فقط لكل جلسة متصفح.
- توسيع كل الصفحات الـ33 التي كانت شبه فارغة (25-30 سطر placeholder)
  بمحتوى حقيقي كامل، مع أقسام صور وفيديو منظمة.
- إصلاح `content-check.yml` الذي كان يتحقق من ملفات HTML ثابتة
  (`pages/team.html` إلخ) لا وجود لها في مشروع React هذا؛ أصبح يتحقق
  من ناتج البناء الفعلي في `dist/`.
- تحديث `sitemap.xml` بالكامل ليعكس الـ39 مساراً الفعلية بدل روابط
  `.html` قديمة من بنية مختلفة تماماً.

## ⏳ متبقٍ (يحتاج مواد من طرفك)

راجع `MEDIA_NEEDED.md` للقائمة الكاملة والدقيقة بأسماء الصور والفيديوهات
والملف الوحيد الناقص (PDF الورقة البيضاء).

## 🔐 أمان — منفصل عن الموقع لكن ذو أولوية قصوى (لم يُغلق بعد)

هذه البنود من سجل سابق ولم يتأكد إغلاقها؛ يُرجى التحقق يدوياً قبل اعتبارها منتهية:
- [ ] التأكد من أن upgrade authority لعقد MD1usd على Solana منقول فعلاً
      لمفتاح آمن جديد (وليس محفظة قديمة قد تكون مخترقة)
- [ ] تأكيد commit hash الصحيح لبدء عملية `solana-verify`
- [ ] مراجعة عنوان MDM1 governance المذكور في `links.md` القديم
      (`...CFCf13c3f`) الذي يختلف بحرف واحد عن العنوان المستخدم فعلياً
      في الموقع والمؤكد في عدة صفحات (`...CFCf12c3f`) — يُرجى التأكد
      يدوياً من عنوان العقد الصحيح على PolygonScan قبل نشر أي منهما.
